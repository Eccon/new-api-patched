# 补丁仓库处理规范

## 仓库边界

- 当前仓库只用于维护补丁，不包含上游源码。
- 禁止将上游源码目录添加、暂存或提交到当前补丁仓库。
- 如果将上游仓库克隆到当前仓库下的 `new-api/`，该目录必须始终保持未跟踪状态。
- 保留当前仓库及已有上游副本中与任务无关的修改。
- 不得覆盖、清理或回退不属于当前任务的未提交修改。

## 工作模式

补丁工作分为“单补丁处理”和“最终整合”两个阶段。默认进入单补丁处理阶段；只有用户明确要求“统一处理上下文漂移”“最终整合”或“完整顺序验证”时，才能进入最终整合阶段。

开始工作时必须说明当前模式、固定的上游 commit、当前补丁编号和使用的临时源码目录。不得把单补丁任务自行扩大成全补丁验证。

## 上游基线

- 每轮连续补丁处理开始时，从 `https://github.com/QuantumNous/new-api` 获取一次最新上游并记录 commit。
- 同一轮工作中，只要该上游 commit 未改变且累计基线可信，应复用已有的干净副本或累计提交，不得无理由重复联网 fetch、clone 或从 `0001` 回放。
- “复用基线”可以是在同一临时仓库继续提交，也可以从已记录的累计提交创建本地分支、worktree 或副本。
- 基线不存在、状态不可信、包含无法保留的修改、上游已经改变，或用户明确要求刷新时，必须重新获取或创建干净副本。
- 不得清理带有未提交修改的已有上游副本；需要隔离时另建可丢弃的临时目录。

## 单补丁处理

处理 `000N` 时遵循以下流程：

1. 准备“固定上游 commit + 已定稿的 `0001` 至 `000N-1`”累计基线，并在临时上游仓库中提交该基线。
2. 只实现、审查和验证当前 `000N`。当前源码改动在用户 audit 前保持未提交，便于直接检查 diff。
3. 不检查、不应用、不测试 `000N+1` 及后续补丁，也不提前修复它们的上下文漂移。
4. 根据当前补丁的风险运行必要且有针对性的测试；不得用全补丁回放代替当前功能测试。
5. 用户 audit 并确认后，才生成或替换正式补丁，并只更新与当前补丁直接相关的 README 内容。
6. 正式补丁生成后，必须在临时上游仓库为当前补丁创建独立的本地提交；下一个补丁只能基于该提交开始开发。
7. 完成当前补丁的本地提交后停止，不做全序列验证；上下文漂移统一留到最终整合阶段处理。

单补丁阶段的完成只表示当前补丁已定稿，不得宣称整个补丁序列可以合入。

## 最终整合

最终整合只能由用户明确触发，并遵循以下流程：

1. 获取一次最新上游，记录远端 HEAD，并创建新的可丢弃干净副本。
2. 确认 `patches/` 中每个编号只有一个活动补丁，再按数字序号处理全部活动补丁。
3. 每个补丁必须先执行 `git apply --check`，通过后再实际应用，然后继续下一个补丁。
4. 遇到纯上下文漂移时，在当前整合副本中修正补丁并继续，不得每修一个 hunk 就重新从 `0001` 回放。
5. 收集并处理完本轮所有纯上下文漂移后，只创建一次新的干净副本，从 `0001` 开始进行最终完整回放。
6. 所有补丁顺序应用成功后执行 `git diff --check`，然后才能报告整个序列可以合入。

## 冲突处理

### 仅上下文漂移

上下文漂移包括代码行移动、文件或函数改名，以及不影响补丁行为的上游重构。

- 只调整路径、定位上下文、hunk 头和 index hash，不得改变补丁语义。
- 不得借机重构、扩展或重新设计功能。
- 单补丁阶段只记录后续补丁的漂移，不处理它。
- 最终整合阶段可以集中修正并继续检查后续补丁。

### 语义或功能冲突

出现以下任一情况时立即停止，不得擅自修改补丁语义：

- 上游已经实现了补丁功能。
- 上游行为与补丁预期存在逻辑冲突。
- 合入需要产品、接口、数据模型或架构决策。
- 目标功能已经删除，且不存在明确的等价位置。
- 只有改变补丁语义才能继续合入。

报告第一个阻断补丁、失败文件或 hunk、相关上游改动及具体逻辑冲突，等待用户决定。

## 测试与工具

- 不在本地执行以产出程序或制品为目的的构建、编译、打包或发布流程，例如 `go build`、发行二进制构建或前端生产打包。
- 必要的本地测试允许执行，包括 `go test` 自身所需的临时编译；测试范围应与当前补丁风险匹配。
- 格式化、静态检查和 `git diff --check` 可以按需执行。
- 如果缺少 Go、gofmt 或其他必要工具，只能把官方工具包下载并解压到独立临时目录，再通过绝对路径或当前命令的临时 `PATH` 调用。
- 禁止使用系统包管理器、`sudo` 或系统目录安装工具，禁止修改全局环境、shell 配置或用户级永久工具链。
- 工具、缓存或依赖需要额外写入时，应使用任务专用的临时目录，不得污染补丁仓库或系统环境。

## 补丁目录

- `patches/` 中同一编号只能保留一个活动补丁。
- 新补丁替代旧补丁时，应在用户确认后删除旧文件并同步 README。
- 如确需保留历史对照，应移到不参与活动补丁 glob 的归档目录，不得与活动补丁混放。
- 补丁名称应使用四位数字序号和简洁、准确的英文 kebab-case 功能描述。

## 提交要求

- 补丁仓库不得自动提交；只有用户明确要求提交时才执行。
- 临时上游仓库允许创建本地提交，用于固定累计基线和生成单一原子补丁。
- 提交补丁仓库时必须显式列出暂存文件，禁止使用 `git add .` 或 `git add -A`。
- 提交前检查 `git diff --cached --name-only`，确保不存在任何 `new-api/` 或其他上游源码路径。
- 遵循仓库已有提交风格，例如：`fix(patches): refresh contexts for latest upstream`。

## 最终报告

单补丁阶段报告：固定的上游 commit、累计基线、当前补丁改动、执行的针对性测试、未执行的验证，以及正式补丁是否已经生成。

最终整合阶段报告：上游获取方式和 commit、全部活动补丁是否按序成功、调整了哪些补丁及原因、最终 `git diff --check` 结果，以及提交 hash（如有）。

所有报告都必须明确说明是否执行了本地测试、未执行本地产物构建，并确认没有把上游源码纳入补丁仓库。
