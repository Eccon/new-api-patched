diff --git a/web/src/features/usage-logs/components/columns/common-logs-columns.tsx b/web/src/features/usage-logs/components/columns/common-logs-columns.tsx
index 58656acf7..fefac4b24 100644
--- a/web/src/features/usage-logs/components/columns/common-logs-columns.tsx
+++ b/web/src/features/usage-logs/components/columns/common-logs-columns.tsx
@@ -108,6 +108,29 @@ function getGroupRatio(other: LogOtherData | null): number | null {
   return null
 }
 
+function formatCacheReadRate(
+  cacheTokens: number,
+  promptTokens: number,
+  other: LogOtherData | null
+): string {
+  const cacheReadTokens = Number(cacheTokens)
+  const inputTokens = Number(promptTokens)
+  const denominator =
+    other?.claude === true ? inputTokens + cacheReadTokens : inputTokens
+  if (
+    !Number.isFinite(cacheReadTokens) ||
+    !Number.isFinite(denominator) ||
+    cacheReadTokens <= 0 ||
+    denominator <= 0
+  ) {
+    return ''
+  }
+
+  const rate = (cacheReadTokens / denominator) * 100
+  const fixed = rate.toFixed(1)
+  return fixed.endsWith('.0') ? `${Math.round(rate)}%` : `${fixed}%`
+}
+
 function buildDetailSegments(
   log: UsageLog,
   other: LogOtherData | null,
@@ -736,6 +759,11 @@ export function useCommonLogsColumns(
           const cacheWriteTokens = hasSplitCache
             ? cacheWrite5m + cacheWrite1h
             : other?.cache_creation_tokens || 0
+          const cacheReadRate =
+            cacheReadTokens > 0
+              ? formatCacheReadRate(cacheReadTokens, promptTokens, other)
+              : ''
+          const cacheReadRateText = cacheReadRate ? ` (${cacheReadRate})` : ''
 
           return (
             <div className='flex flex-col gap-0.5'>
@@ -748,6 +776,7 @@ export function useCommonLogsColumns(
                   {cacheReadTokens > 0 && (
                     <span className='text-muted-foreground/60'>
                       {t('Cache')}↓ {cacheReadTokens.toLocaleString()}
+                      {cacheReadRateText}
                     </span>
                   )}
                   {cacheWriteTokens > 0 && (
