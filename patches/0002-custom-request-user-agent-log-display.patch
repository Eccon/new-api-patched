diff --git a/model/log.go b/model/log.go
index a0407d53..edd73b4b 100644
--- a/model/log.go
+++ b/model/log.go
@@ -6,6 +6,7 @@ import (
 	"fmt"
 	"strings"
 	"time"
+	"unicode/utf8"
 
 	"github.com/QuantumNous/new-api/common"
 	"github.com/QuantumNous/new-api/logger"
@@ -16,6 +17,8 @@ import (
 	"gorm.io/gorm"
 )
 
+const maxLoggedUserAgentLength = 512
+
 func applyExplicitLogTextFilter(tx *gorm.DB, column string, value string) (*gorm.DB, error) {
 	if value == "" {
 		return tx, nil
@@ -281,6 +284,7 @@ func RecordErrorLog(c *gin.Context, userId int, channelId int, modelName string,
 	username := c.GetString("username")
 	requestId := c.GetString(common.RequestIdKey)
 	upstreamRequestId := c.GetString(common.UpstreamRequestIdKey)
+	other = appendRequestUserAgentToOther(c, other)
 	otherStr := other.JSONString()
 	// 判断是否需要记录 IP
 	needRecordIp := false
@@ -336,6 +340,36 @@ type RecordConsumeLogParams struct {
 	Other            *LogOther `json:"other"`
 }
 
+func appendRequestUserAgentToOther(c *gin.Context, other *LogOther) *LogOther {
+	if c == nil || c.Request == nil {
+		return other
+	}
+	userAgent := strings.TrimSpace(c.Request.UserAgent())
+	if userAgent == "" {
+		return other
+	}
+	userAgent = truncateUserAgent(userAgent)
+	if userAgent == "" {
+		return other
+	}
+	if other == nil {
+		other = NewLogOther()
+	}
+	other.SetPublic("request_user_agent", userAgent)
+	return other
+}
+
+func truncateUserAgent(userAgent string) string {
+	if len(userAgent) <= maxLoggedUserAgentLength {
+		return userAgent
+	}
+	userAgent = userAgent[:maxLoggedUserAgentLength]
+	for len(userAgent) > 0 && !utf8.ValidString(userAgent) {
+		userAgent = userAgent[:len(userAgent)-1]
+	}
+	return userAgent
+}
+
 func RecordConsumeLog(c *gin.Context, userId int, params RecordConsumeLogParams) {
 	if !common.LogConsumeEnabled {
 		return
@@ -345,6 +379,7 @@ func RecordConsumeLog(c *gin.Context, userId int, params RecordConsumeLogParams)
 	requestId := c.GetString(common.RequestIdKey)
 	upstreamRequestId := c.GetString(common.UpstreamRequestIdKey)
 	createdAt := common.GetTimestamp()
+	params.Other = appendRequestUserAgentToOther(c, params.Other)
 	otherStr := params.Other.JSONString()
 	// 判断是否需要记录 IP
 	needRecordIp := false
diff --git a/web/src/features/usage-logs/components/dialogs/details-dialog.tsx b/web/src/features/usage-logs/components/dialogs/details-dialog.tsx
index 8b7861db..0e7eef32 100644
--- a/web/src/features/usage-logs/components/dialogs/details-dialog.tsx
+++ b/web/src/features/usage-logs/components/dialogs/details-dialog.tsx
@@ -526,6 +526,10 @@ export function DetailsDialog(props: DetailsDialogProps) {
   const showTiming = isTimingLogType(props.log.type)
   const showAdminIp =
     !!props.log.ip && (showTiming || (props.isAdmin && isTopup))
+  const requestUserAgent =
+    typeof other?.request_user_agent === 'string'
+      ? other.request_user_agent.trim()
+      : ''
   const adminInfo = other?.admin_info
   const topupAuditFields =
     isTopup && props.isAdmin && adminInfo
@@ -728,6 +732,22 @@ export function DetailsDialog(props: DetailsDialogProps) {
             />
           )}
 
+          {requestUserAgent && (
+            <DetailRow
+              label={t('User-Agent')}
+              value={
+                <button
+                  type='button'
+                  className='max-w-full cursor-pointer text-left break-all hover:underline'
+                  onClick={() => copyToClipboard(requestUserAgent)}
+                >
+                  {requestUserAgent}
+                </button>
+              }
+              mono
+            />
+          )}
+
           {showTiming && props.log.use_time > 0 && (
             <DetailRow
               label={t('Response Time')}
diff --git a/web/src/features/usage-logs/types.ts b/web/src/features/usage-logs/types.ts
index 26a122e6..3a7246ce 100644
--- a/web/src/features/usage-logs/types.ts
+++ b/web/src/features/usage-logs/types.ts
@@ -169,6 +169,7 @@ export interface LogOtherData {
   // Login audit fields (type=7); visible to the log owner
   login_method?: string
   user_agent?: string
+  request_user_agent?: string
   request_path?: string
   request_conversion?: string[]
   ws?: boolean
diff --git a/web/src/i18n/locales/en.json b/web/src/i18n/locales/en.json
index 64b6c97c..acc06506 100644
--- a/web/src/i18n/locales/en.json
+++ b/web/src/i18n/locales/en.json
@@ -5286,6 +5286,7 @@
     "User Subscription Management": "User Subscription Management",
     "User updated successfully": "User updated successfully",
     "User Verification": "User Verification",
+    "User-Agent": "User-Agent",
     "User-Agent include (one per line)": "User-Agent include (one per line)",
     "Username": "Username",
     "Username confirmation does not match": "Username confirmation does not match",
diff --git a/web/src/i18n/locales/fr.json b/web/src/i18n/locales/fr.json
index b68f6212..5ceeaf77 100644
--- a/web/src/i18n/locales/fr.json
+++ b/web/src/i18n/locales/fr.json
@@ -5286,6 +5286,7 @@
     "User Subscription Management": "Gestion des abonnements utilisateur",
     "User updated successfully": "Utilisateur mis à jour avec succès",
     "User Verification": "Vérification de l'utilisateur",
+    "User-Agent": "User-Agent",
     "User-Agent include (one per line)": "User-Agent inclus (un par ligne)",
     "Username": "Nom d'utilisateur",
     "Username confirmation does not match": "La confirmation du nom d'utilisateur ne correspond pas",
diff --git a/web/src/i18n/locales/ja.json b/web/src/i18n/locales/ja.json
index 631c54d5..dacc6217 100644
--- a/web/src/i18n/locales/ja.json
+++ b/web/src/i18n/locales/ja.json
@@ -5286,6 +5286,7 @@
     "User Subscription Management": "ユーザーサブスクリプション管理",
     "User updated successfully": "ユーザーの更新に成功しました",
     "User Verification": "ユーザー認証",
+    "User-Agent": "User-Agent",
     "User-Agent include (one per line)": "User-Agent include（1行に1つ）",
     "Username": "ユーザー名",
     "Username confirmation does not match": "ユーザー名の確認が一致しません",
diff --git a/web/src/i18n/locales/ru.json b/web/src/i18n/locales/ru.json
index aeabf44f..793e00fa 100644
--- a/web/src/i18n/locales/ru.json
+++ b/web/src/i18n/locales/ru.json
@@ -5286,6 +5286,7 @@
     "User Subscription Management": "Управление подписками пользователя",
     "User updated successfully": "Пользователь успешно обновлен",
     "User Verification": "Проверка пользователя",
+    "User-Agent": "User-Agent",
     "User-Agent include (one per line)": "User-Agent include (по одному на строку)",
     "Username": "Имя пользователя",
     "Username confirmation does not match": "Подтверждение имени пользователя не совпадает",
diff --git a/web/src/i18n/locales/vi.json b/web/src/i18n/locales/vi.json
index 40fc633b..cd1c918b 100644
--- a/web/src/i18n/locales/vi.json
+++ b/web/src/i18n/locales/vi.json
@@ -5286,6 +5286,7 @@
     "User Subscription Management": "Quản lý đăng ký người dùng",
     "User updated successfully": "Cập nhật người dùng thành công",
     "User Verification": "Xác minh người dùng",
+    "User-Agent": "User-Agent",
     "User-Agent include (one per line)": "User-Agent include (mỗi dòng một mục)",
     "Username": "Tên người dùng",
     "Username confirmation does not match": "Xác nhận tên người dùng không khớp",
diff --git a/web/src/i18n/locales/zh.json b/web/src/i18n/locales/zh.json
index c963eb8e..25173ea4 100644
--- a/web/src/i18n/locales/zh.json
+++ b/web/src/i18n/locales/zh.json
@@ -5286,6 +5286,7 @@
     "User Subscription Management": "用户订阅管理",
     "User updated successfully": "用户更新成功",
     "User Verification": "用户验证",
+    "User-Agent": "User-Agent",
     "User-Agent include (one per line)": "User-Agent include（每行一个）",
     "Username": "用户名",
     "Username confirmation does not match": "用户名确认不匹配",
