diff --git a/relay/channel/openai/chat_via_responses.go b/relay/channel/openai/chat_via_responses.go
index 1bc04f67a3488c4bb6638ade6c90570f9cdf1884..e9ba1fe2880d0dd697a85e41464c595bc2dcbf25 100644
--- a/relay/channel/openai/chat_via_responses.go
+++ b/relay/channel/openai/chat_via_responses.go
@@ -36,6 +36,7 @@ func OaiResponsesToChatHandler(c *gin.Context, info *relaycommon.RelayInfo, resp
 	if err := common.Unmarshal(body, &responsesResp); err != nil {
 		return nil, types.NewOpenAIError(err, types.ErrorCodeBadResponseBody, http.StatusInternalServerError)
 	}
+	info.SetResponseModelName(responsesResp.Model)
 
 	if oaiError := responsesResp.GetOpenAIError(); oaiError != nil && oaiError.Type != "" {
 		return nil, types.WithOpenAIError(*oaiError, resp.StatusCode)
@@ -105,6 +106,9 @@ func OaiResponsesToChatBufferedStreamHandler(c *gin.Context, info *relaycommon.R
 			break
 		}
 		accumulator.ProcessEvent(&streamResp)
+		if streamResp.Response != nil && streamResp.Response.Model != "" {
+			info.SetResponseModelName(streamResp.Response.Model)
+		}
 		switch streamResp.Type {
 		case "response.completed", "response.done", "response.incomplete":
 			finalResponse = streamResp.Response
@@ -229,6 +233,9 @@ func OaiResponsesToChatStreamHandler(c *gin.Context, info *relaycommon.RelayInfo
 			sr.Error(err)
 			return
 		}
+		if streamResp.Response != nil && streamResp.Response.Model != "" {
+			info.SetResponseModelName(streamResp.Response.Model)
+		}
 
 		if streamResp.Type == "response.error" || streamResp.Type == "response.failed" {
 			if streamResp.Response != nil {
diff --git a/relay/channel/openai/helper.go b/relay/channel/openai/helper.go
index ed1f59d896f9b8bfced62bebde5f259e8fd999a1..d2c5ea67c11c61388346c416c33aa40bc5d0db36 100644
--- a/relay/channel/openai/helper.go
+++ b/relay/channel/openai/helper.go
@@ -91,22 +91,22 @@ func ProcessStreamResponse(streamResponse dto.ChatCompletionsStreamResponse, res
 	return nil
 }
 
-func processTokenData(relayMode int, data string, responseTextBuilder *strings.Builder, toolCount *int) error {
+func processTokenData(relayMode int, data string, responseTextBuilder *strings.Builder, toolCount *int) (string, error) {
 	switch relayMode {
 	case relayconstant.RelayModeChatCompletions:
 		var streamResponse dto.ChatCompletionsStreamResponse
 		if err := common.UnmarshalJsonStr(data, &streamResponse); err != nil {
-			return err
+			return "", err
 		}
-		return ProcessStreamResponse(streamResponse, responseTextBuilder, toolCount)
+		return streamResponse.Model, ProcessStreamResponse(streamResponse, responseTextBuilder, toolCount)
 	case relayconstant.RelayModeCompletions:
 		var streamResponse dto.CompletionsStreamResponse
 		if err := common.UnmarshalJsonStr(data, &streamResponse); err != nil {
-			return err
+			return "", err
 		}
 		processCompletionsStreamResponse(streamResponse, responseTextBuilder)
 	}
-	return nil
+	return "", nil
 }
 
 func processCompletionsStreamResponse(streamResponse dto.CompletionsStreamResponse, responseTextBuilder *strings.Builder) {
@@ -128,7 +128,9 @@ func handleLastResponse(lastStreamData string, responseId *string, createAt *int
 	*responseId = lastStreamResponse.Id
 	*createAt = lastStreamResponse.Created
 	*systemFingerprint = lastStreamResponse.GetSystemFingerprint()
-	*model = lastStreamResponse.Model
+	if lastStreamResponse.Model != "" {
+		*model = lastStreamResponse.Model
+	}
 
 	if service.ValidUsage(lastStreamResponse.Usage) {
 		*containStreamUsage = true
diff --git a/relay/channel/openai/relay-openai.go b/relay/channel/openai/relay-openai.go
index de40fe7071fc14e4042799851b6e6842ffee2420..c2c4c396cc8c5dc75961b9d51336afb45d8a56e0 100644
--- a/relay/channel/openai/relay-openai.go
+++ b/relay/channel/openai/relay-openai.go
@@ -118,6 +118,7 @@ func OaiStreamHandler(c *gin.Context, info *relaycommon.RelayInfo, resp *http.Re
 	var usage = &dto.Usage{}
 	var lastStreamData string
 	var secondLastStreamData string // 存储倒数第二个stream data，用于音频模型
+	capturedResponseModel := false
 
 	// 检查是否为音频模型
 	isAudioModel := strings.Contains(strings.ToLower(model), "audio")
@@ -136,10 +137,16 @@ func OaiStreamHandler(c *gin.Context, info *relaycommon.RelayInfo, resp *http.Re
 			}
 
 			lastStreamData = data
-			if err := processTokenData(info.RelayMode, data, &responseTextBuilder, &toolCount); err != nil {
+			streamModel, err := processTokenData(info.RelayMode, data, &responseTextBuilder, &toolCount)
+			if err != nil {
 				logger.LogError(c, "error processing stream token data: "+err.Error())
 				sr.Error(err)
 			}
+			if !capturedResponseModel && strings.TrimSpace(streamModel) != "" {
+				model = streamModel
+				info.SetResponseModelName(streamModel)
+				capturedResponseModel = true
+			}
 		}
 	})
 
@@ -215,6 +222,7 @@ func OpenaiHandler(c *gin.Context, info *relaycommon.RelayInfo, resp *http.Respo
 	if err != nil {
 		return nil, types.NewOpenAIError(err, types.ErrorCodeBadResponseBody, http.StatusInternalServerError)
 	}
+	info.SetResponseModelName(simpleResponse.Model)
 
 	if oaiError := simpleResponse.GetOpenAIError(); oaiError != nil && oaiError.Type != "" {
 		return nil, types.WithOpenAIError(*oaiError, resp.StatusCode)
diff --git a/relay/channel/openai/relay_responses.go b/relay/channel/openai/relay_responses.go
index 8c9d421d3d2bfac5a90189bfb695dedb69140cf1..7effcf181d34fc344f123e3703cc2a63593a18c1 100644
--- a/relay/channel/openai/relay_responses.go
+++ b/relay/channel/openai/relay_responses.go
@@ -30,6 +30,7 @@ func OaiResponsesHandler(c *gin.Context, info *relaycommon.RelayInfo, resp *http
 	if err != nil {
 		return nil, types.NewOpenAIError(err, types.ErrorCodeBadResponseBody, http.StatusInternalServerError)
 	}
+	info.SetResponseModelName(responsesResponse.Model)
 	if oaiError := responsesResponse.GetOpenAIError(); oaiError != nil && oaiError.Type != "" {
 		return nil, types.WithOpenAIError(*oaiError, resp.StatusCode)
 	}
@@ -94,6 +95,9 @@ func OaiResponsesStreamHandler(c *gin.Context, info *relaycommon.RelayInfo, resp
 		if streamResponse.SequenceNumber != nil {
 			lastSequenceNumber = *streamResponse.SequenceNumber
 		}
+		if info.ResponseModelName == "" && streamResponse.Response != nil {
+			info.SetResponseModelName(streamResponse.Response.Model)
+		}
 		sendResponsesStreamData(c, streamResponse, data)
 		switch streamResponse.Type {
 		case "response.completed":
diff --git a/relay/common/relay_info.go b/relay/common/relay_info.go
index e938da61b50d99e49d8ac42dec94ba9b7ced1f10..2b0819aff483170309584cc4add58079b7c5266f 100644
--- a/relay/common/relay_info.go
+++ b/relay/common/relay_info.go
@@ -183,6 +183,7 @@ type RelayInfo struct {
 	UsePrice               bool
 	RelayMode              int
 	OriginModelName        string
+	ResponseModelName      string
 	RequestURLPath         string
 	RequestHeaders         map[string]string
 	ShouldIncludeUsage     bool
@@ -742,6 +743,20 @@ func (info *RelayInfo) GetEstimatePromptTokens() int {
 	return info.estimatePromptTokens
 }
 
+func (info *RelayInfo) SetResponseModelName(modelName string) {
+	if info == nil {
+		return
+	}
+	modelName = strings.TrimSpace(modelName)
+	if modelName == "" {
+		return
+	}
+	if info.ResponseModelName != "" {
+		return
+	}
+	info.ResponseModelName = modelName
+}
+
 func (info *RelayInfo) SetFirstResponseTime() {
 	info.SetFirstResponseTimeAt(time.Now())
 }
diff --git a/service/log_info_generate.go b/service/log_info_generate.go
index 207b0af5bfefcbf339547a4dff28cbf8f7b3c4ba..a5e50abe5cafb996e5fa88400d911dd9e0fd0c8f 100644
--- a/service/log_info_generate.go
+++ b/service/log_info_generate.go
@@ -68,6 +68,26 @@ func appendRequestPath(ctx *gin.Context, relayInfo *relaycommon.RelayInfo, other
 	}
 }
 
+func appendActualModelInfo(relayInfo *relaycommon.RelayInfo, other map[string]interface{}) {
+	if relayInfo == nil || other == nil {
+		return
+	}
+
+	requestModel := strings.TrimSpace(relayInfo.OriginModelName)
+	mappedModel := strings.TrimSpace(relayInfo.UpstreamModelName)
+	responseModel := strings.TrimSpace(relayInfo.ResponseModelName)
+
+	if responseModel != "" && (responseModel != requestModel || (mappedModel != "" && responseModel != mappedModel)) {
+		other["response_model_name"] = responseModel
+		other["actual_model_name"] = responseModel
+		return
+	}
+
+	if relayInfo.IsModelMapped && mappedModel != "" && mappedModel != requestModel {
+		other["actual_model_name"] = mappedModel
+	}
+}
+
 func GenerateTextOtherInfo(ctx *gin.Context, relayInfo *relaycommon.RelayInfo, modelRatio, groupRatio, completionRatio float64,
 	cacheTokens int, cacheRatio float64, modelPrice float64, userGroupRatio float64) map[string]interface{} {
 	other := make(map[string]interface{})
@@ -86,6 +106,7 @@ func GenerateTextOtherInfo(ctx *gin.Context, relayInfo *relaycommon.RelayInfo, m
 		other["is_model_mapped"] = true
 		other["upstream_model_name"] = relayInfo.UpstreamModelName
 	}
+	appendActualModelInfo(relayInfo, other)
 
 	isSystemPromptOverwritten := common.GetContextKeyBool(ctx, constant.ContextKeySystemPromptOverride)
 	if isSystemPromptOverwritten {
diff --git a/web/classic/src/components/table/usage-logs/UsageLogsColumnDefs.jsx b/web/classic/src/components/table/usage-logs/UsageLogsColumnDefs.jsx
index baf9ba27bc7705b47a50c0e4a15f1068e02e4f2d..cb8ac9962a0db56df6f5698ad7191a63d510c6d6 100644
--- a/web/classic/src/components/table/usage-logs/UsageLogsColumnDefs.jsx
+++ b/web/classic/src/components/table/usage-logs/UsageLogsColumnDefs.jsx
@@ -275,7 +275,13 @@ function renderModelName(record, copyText, t) {
     other?.is_model_mapped &&
     other?.upstream_model_name &&
     other?.upstream_model_name !== '';
-  if (!modelMapped) {
+  let actualModel =
+    other?.actual_model_name && other.actual_model_name !== ''
+      ? other.actual_model_name
+      : modelMapped
+        ? other.upstream_model_name
+        : '';
+  if (!actualModel) {
     return renderModelTag(record.model_name, {
       onClick: (event) => {
         copyText(event, record.model_name).then((r) => {});
@@ -303,9 +309,9 @@ function renderModelName(record, copyText, t) {
                     <Typography.Text strong style={{ marginRight: 8 }}>
                       {t('实际模型')}:
                     </Typography.Text>
-                    {renderModelTag(other.upstream_model_name, {
+                    {renderModelTag(actualModel, {
                       onClick: (event) => {
-                        copyText(event, other.upstream_model_name).then(
+                        copyText(event, actualModel).then(
                           (r) => {},
                         );
                       },
diff --git a/web/default/src/features/usage-logs/components/dialogs/details-dialog.tsx b/web/default/src/features/usage-logs/components/dialogs/details-dialog.tsx
index 41043efebe1232381006fcf2c6214a23fd779638..078a32110f43049ffdaf4bdca28d9a7c81c8a4b2 100644
--- a/web/default/src/features/usage-logs/components/dialogs/details-dialog.tsx
+++ b/web/default/src/features/usage-logs/components/dialogs/details-dialog.tsx
@@ -419,6 +419,9 @@ export function DetailsDialog(props: DetailsDialogProps) {
   } else if (other?.reasoning_effort === 'medium') {
     reasoningEffortVariant = 'info'
   }
+  const actualModel =
+    other?.actual_model_name?.trim() ||
+    (other?.is_model_mapped ? other?.upstream_model_name : undefined)
 
   const isViolation = isViolationFeeLog(other)
   const isRefund = props.log.type === 6
@@ -978,7 +981,7 @@ export function DetailsDialog(props: DetailsDialogProps) {
         )}
 
         {/* Model mapping */}
-        {other?.is_model_mapped && other?.upstream_model_name && (
+        {actualModel && (
           <DetailSection label={t('Model Mapping')}>
             <DetailRow
               label={t('Request Model')}
@@ -987,7 +990,7 @@ export function DetailsDialog(props: DetailsDialogProps) {
             />
             <DetailRow
               label={t('Actual Model')}
-              value={other.upstream_model_name}
+              value={actualModel}
               mono
             />
           </DetailSection>
diff --git a/web/default/src/features/usage-logs/lib/format.ts b/web/default/src/features/usage-logs/lib/format.ts
index e00eb1b435a88fad701f662d8858094805d8f63b..fdff5c3500a94f11777649d1bf142ca063b55636 100644
--- a/web/default/src/features/usage-logs/lib/format.ts
+++ b/web/default/src/features/usage-logs/lib/format.ts
@@ -159,6 +159,7 @@ export function formatModelName(log: UsageLog): {
   actualModel?: string
 } {
   const other = parseLogOther(log.other)
+  const actualModel = other?.actual_model_name?.trim()
   const isMapped = !!(
     other?.is_model_mapped &&
     other?.upstream_model_name &&
@@ -168,7 +169,7 @@ export function formatModelName(log: UsageLog): {
   return {
     name: log.model_name,
     isMapped,
-    actualModel: isMapped ? other.upstream_model_name : undefined,
+    actualModel: actualModel || (isMapped ? other.upstream_model_name : undefined),
   }
 }
 
diff --git a/web/default/src/features/usage-logs/types.ts b/web/default/src/features/usage-logs/types.ts
index 7640261644627000927307710c2d83232946a238..71c254f2d0a86fd74a9ec18b458be73846583dfa 100644
--- a/web/default/src/features/usage-logs/types.ts
+++ b/web/default/src/features/usage-logs/types.ts
@@ -164,6 +164,7 @@ export interface LogOtherData {
   cache_creation_ratio_1h?: number
   is_model_mapped?: boolean
   upstream_model_name?: string
+  actual_model_name?: string
   audio_ratio?: number
   audio_completion_ratio?: number
   frt?: number
