diff --git a/relay/channel/openai/helper.go b/relay/channel/openai/helper.go
index e52014e26..e13069e05 100644
--- a/relay/channel/openai/helper.go
+++ b/relay/channel/openai/helper.go
@@ -268,3 +268,42 @@ func sendResponsesStreamData(c *gin.Context, streamResponse dto.ResponsesStreamR
 	}
 	_ = helper.ResponseChunkData(c, streamResponse, data)
 }
+
+func sendResponsesErrorEvent(c *gin.Context, reason string, sequenceNumber int) {
+	payload := struct {
+		Type           string  `json:"type"`
+		SequenceNumber int     `json:"sequence_number"`
+		Code           string  `json:"code"`
+		Message        string  `json:"message"`
+		Param          *string `json:"param"`
+	}{
+		Type:           "error",
+		SequenceNumber: sequenceNumber,
+		Code:           reason,
+		Message:        reason,
+		Param:          nil,
+	}
+
+	if c == nil || c.Writer == nil {
+		return
+	}
+	if c.Request != nil && c.Request.Context().Err() != nil {
+		return
+	}
+
+	jsonData, err := common.Marshal(payload)
+	if err != nil {
+		logger.LogError(c, "failed to marshal responses error event: "+err.Error())
+		return
+	}
+
+	helper.ExtendWriteDeadline(c)
+	c.Render(-1, common.CustomEvent{Data: "event: error\n"})
+	c.Render(-1, common.CustomEvent{Data: "data: " + string(jsonData)})
+	if err := helper.FlushWriter(c); err != nil {
+		if c != nil && c.Request != nil && c.Request.Context().Err() != nil {
+			return
+		}
+		logger.LogError(c, "failed to send responses error event: "+err.Error())
+	}
+}
diff --git a/relay/channel/openai/relay_responses.go b/relay/channel/openai/relay_responses.go
index 3ed0b3bed..12f03ca69 100644
--- a/relay/channel/openai/relay_responses.go
+++ b/relay/channel/openai/relay_responses.go
@@ -78,6 +78,9 @@ func OaiResponsesStreamHandler(c *gin.Context, info *relaycommon.RelayInfo, resp
 	defer service.CloseResponseBodyGracefully(resp)
 
 	accumulator := service.NewResponsesUsageAccumulator(info)
+	sawTerminalEvent := false
+	sawErrorEvent := false
+	lastSequenceNumber := -1
 
 	helper.StreamScannerHandler(c, resp, info, func(data string, sr *helper.StreamResult) {
 
@@ -88,12 +91,33 @@ func OaiResponsesStreamHandler(c *gin.Context, info *relaycommon.RelayInfo, resp
 			sr.Error(err)
 			return
 		}
+		if streamResponse.SequenceNumber != nil {
+			lastSequenceNumber = *streamResponse.SequenceNumber
+		}
 		if streamResponse.Response != nil {
 			data = string(rewriteSGLangResponsesCreatedAt(info, []byte(data), "response.created_at", streamResponse.Response.CreatedAt))
 		}
 		sendResponsesStreamData(c, streamResponse, data)
 		accumulator.Observe(&streamResponse)
+		switch streamResponse.Type {
+		case "response.completed", "response.done":
+			sawTerminalEvent = true
+		case "error", "response.error", "response.failed", "response.incomplete", "response.cancelled", "response.canceled":
+			sawErrorEvent = true
+		}
 	})
+	if info != nil && info.StreamStatus != nil && !sawTerminalEvent && !sawErrorEvent {
+		nextSequenceNumber := 0
+		if lastSequenceNumber >= 0 {
+			nextSequenceNumber = lastSequenceNumber + 1
+		}
+		switch info.StreamStatus.EndReason {
+		case relaycommon.StreamEndReasonTimeout:
+			sendResponsesErrorEvent(c, "stream_timeout", nextSequenceNumber)
+		case relaycommon.StreamEndReasonScannerErr:
+			sendResponsesErrorEvent(c, "stream_read_error", nextSequenceNumber)
+		}
+	}
 
 	common.SetContextKey(c, constant.ContextKeyResponseStreamStatus, info.StreamStatus)
 	info.StreamStatus.RequireTerminal()
diff --git a/relay/common/stream_status.go b/relay/common/stream_status.go
index 60c849564..955c040e6 100644
--- a/relay/common/stream_status.go
+++ b/relay/common/stream_status.go
@@ -12,7 +12,7 @@ type StreamEndReason string
 const (
 	StreamEndReasonNone        StreamEndReason = ""
 	StreamEndReasonDone        StreamEndReason = "done"
-	StreamEndReasonTimeout     StreamEndReason = "timeout"
+	StreamEndReasonTimeout     StreamEndReason = "stream_inactivity_timeout"
 	StreamEndReasonClientGone  StreamEndReason = "client_gone"
 	StreamEndReasonScannerErr  StreamEndReason = "scanner_error"
 	StreamEndReasonHandlerStop StreamEndReason = "handler_stop"
diff --git a/relay/common/stream_status_test.go b/relay/common/stream_status_test.go
index 4936b3a06..d75f2ca67 100644
--- a/relay/common/stream_status_test.go
+++ b/relay/common/stream_status_test.go
@@ -171,7 +171,7 @@ func TestStreamStatus_Summary(t *testing.T) {
 	s2.RecordError("bad json")
 	s2.RecordError("write failed")
 	summary2 := s2.Summary()
-	assert.Contains(t, summary2, "reason=timeout")
+	assert.Contains(t, summary2, "reason=stream_inactivity_timeout")
 	assert.Contains(t, summary2, "soft_errors=2")
 }
 
