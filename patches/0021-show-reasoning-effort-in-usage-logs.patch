diff --git a/web/src/features/usage-logs/components/columns/common-logs-columns.tsx b/web/src/features/usage-logs/components/columns/common-logs-columns.tsx
index eb73169fe..ba71caedc 100644
--- a/web/src/features/usage-logs/components/columns/common-logs-columns.tsx
+++ b/web/src/features/usage-logs/components/columns/common-logs-columns.tsx
@@ -44,6 +44,7 @@ import { LOG_TYPE_ALL_VALUE } from '../../constants'
 import type { UsageLog } from '../../data/schema'
 import {
   formatModelName,
+  getReasoningEffortVariant,
   getTieredBillingSummary,
   hasAnyCacheTokens,
   parseLogOther,
@@ -651,6 +652,30 @@ export function useCommonLogsColumns(
       },
       meta: { mobileTitle: true },
     },
+    {
+      id: 'reasoning_effort',
+      header: t('Reasoning Effort'),
+      accessorFn: (row) =>
+        parseLogOther(row.other)?.reasoning_effort?.trim() || '',
+      cell: ({ row }) => {
+        const log = row.original
+        if (!isDisplayableLogType(log.type)) return null
+
+        const effort = parseLogOther(log.other)?.reasoning_effort?.trim()
+        if (!effort) return null
+
+        return (
+          <StatusBadge
+            label={effort}
+            variant={getReasoningEffortVariant(effort)}
+            size='sm'
+            copyable={false}
+          />
+        )
+      },
+      meta: { label: t('Reasoning Effort') },
+      enableSorting: false,
+    },
     {
       accessorKey: 'is_stream',
       header: t('Stream'),
diff --git a/web/src/features/usage-logs/components/usage-logs-mobile-card.tsx b/web/src/features/usage-logs/components/usage-logs-mobile-card.tsx
index ab473d915..275c8e49f 100644
--- a/web/src/features/usage-logs/components/usage-logs-mobile-card.tsx
+++ b/web/src/features/usage-logs/components/usage-logs-mobile-card.tsx
@@ -318,13 +318,17 @@ function CommonLogsCard<TData>({
   const { t } = useTranslation()
 
   const modelCell = cells.get('model_name')
+  const reasoningEffortCell = cells.get('reasoning_effort')
   const quotaCell = cells.get('quota')
   const rowData = cells.get('created_at')?.row.original as UsageLog | undefined
 
   return (
     <div className='space-y-2.5'>
       <div className='flex min-w-0 items-center justify-between gap-3'>
-        <CompactCell cell={modelCell} className='flex-1' />
+        <div className='flex min-w-0 flex-1 items-center gap-1.5'>
+          <CompactCell cell={modelCell} />
+          <CompactCell cell={reasoningEffortCell} className='shrink-0' />
+        </div>
         <CompactCell
           cell={quotaCell}
           className='shrink-0 text-right [&_.flex-col]:items-end'
